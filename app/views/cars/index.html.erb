<h1>Available Cars</h1>


<ul>
  <% @cars.each do |car| %>
    <li><%= link_to car.name, car_path(car)%></i>
  <% end %>
</ul>

<p class="btn btn-warning"><%= link_to "Create new car", new_car_path%></a>
