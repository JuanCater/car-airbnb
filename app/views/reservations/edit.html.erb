<% if @reservation.status == "pending" %>
  <h1>Edit My Reservation Request</h1>
<% end %>

<%= simple_form_for [@car, @reservation] do |f| %>
  <%= f.input :rented_from %>
  <%= f.input :rented_until %>
    <% if @car.user == current_user %>
  <%= f.input :status, collection: ["pending", "accepted", "denied"] %>
  <% end %>
  <%= f.button :submit %>
<% end %>
